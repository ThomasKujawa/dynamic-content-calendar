# dynamic-content-calendar
Interactive, advent-calendar-like website that shows dynamically mapped content items (author, text, link, image) behind randomly placed paired number buttons (light/dark variants).
